Dropdown menus (HTML,CSS,JS)

Dropdown menu examples. One built using CSS definitions (requires mouse cursor over menu element) and other using jQuery and CSS (toggle click property on menu element).
